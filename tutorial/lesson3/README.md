# Lesson 3 - Modify the <Root> component

Let's start by modifying the <Root> component in the /packages/jsnation-theme/src/index.js file so that it returns a <h1> containing the text “Frontity Workshop”.